node_modules
.next
out
.env.local
npm-debug.log*

# 模拟器 ROM 二进制(版权文件不入 GitHub,仅部署同步到自有服务器)
public/roms/*
!public/roms/roms.json
!public/roms/README.md

# EmulatorJS 自托管运行时(开源内核,~20MB,部署 rsync 同步,不入 GitHub)
public/emulator/data
